
import 'package:flutter/material.dart';

import 'components/body.dart';

class NoPageFoundPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      
      child: Center(child: Text('404 PAGINA NO ENCONTRADA'),),
    );
  }
}